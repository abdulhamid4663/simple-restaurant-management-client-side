import { useParams } from "react-router-dom";


const OrderPage = () => {
    const params = useParams()
    console.log(params);
    return (
        <div>
            
        </div>
    );
};

export default OrderPage;